const express = require("express")
const router = express.Router();

// module.exports = ( addUserFavourite ) => {

//   router.get("/", (req, res) => {
//     addUserFavourite()
//       .then((favs) => {
//         const favPets = addUserFavourite(favs);
//         res.json(favPets);
//       })
//       .catch((err) =>
//         res.json({
//           error: err.message,
//         })
//       );
//   });
// return router
// }
